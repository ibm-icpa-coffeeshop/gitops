apiVersion: tekton.dev/v1alpha1
kind: PipelineRun
metadata:
  name: run-deploy-pipeline # TODO use generateName
spec:
  serviceAccountName: pipeline-service-account
  pipelineRef:
    name: pipeline-deploy
  resources:
    - name: git-source
      resourceRef:
        name: gitops-git
  params:
    - name: pathToYaml
      value: "coffeeshop/base"